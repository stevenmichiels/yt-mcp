# Optional for non-default credential paths. Copy to .env and replace the paths.
# The Google account is selected interactively during OAuth; no email variable is needed.
YTMUSIC_OAUTH_CLIENT_FILE=/absolute/path/to/yt-mcp/oauth-client.json
YTMUSIC_AUTH_FILE=/absolute/path/to/yt-mcp/oauth.json
